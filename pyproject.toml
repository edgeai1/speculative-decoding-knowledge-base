[project]
name = "speculative-decoding-knowledge-base"
version = "0.1.0"
description = "Build dependencies for the Speculative Decoding research knowledge base"
requires-python = ">=3.11"
dependencies = [
  "mkdocs-material>=9.6,<10",
]

[tool.uv]
package = false
